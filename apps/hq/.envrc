# HQ App - Automatic Virtual Environment Activation
# This file is used by direnv to automatically activate the .venv when you cd into this directory
# Note: HQ is primarily Laravel/PHP, but may have Python components in the future

# Check if .venv exists, if not, create it (for future Python components)
if [ ! -d .venv ]; then
    echo "Creating .venv for HQ (Python components)..."
    python3 -m venv .venv
fi

# Activate the virtual environment (if it exists)
if [ -d .venv ]; then
    source .venv/bin/activate
fi

# Load environment variables if .env exists
if [ -f .env ]; then
    dotenv
fi

